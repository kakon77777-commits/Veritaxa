# Veritaxa v1.0 release scope

The GitHub reproducible payload includes the complete text source and the XLSX governance workbench. The deterministic Research Bundle implementation, validation contract, and regression tests are contained in the source snapshot.
